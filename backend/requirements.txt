fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0.36
pydantic>=2.9
pydantic-settings>=2.5
pyjwt>=2.9
pytest>=8.3
httpx>=0.27

# --- S3: RAG (local, no API key) ---
# Install torch from the CPU wheel index first — the default PyPI wheel bundles
# CUDA and is ~10x larger:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
sentence-transformers>=3.0
chromadb>=0.5

# --- S13: admin panel ---
# multipart/form-data parsing for the document upload endpoint
python-multipart>=0.0.9

# --- S14: Gemini provider ---
# Google Gemini SDK, used only when LLM_PROVIDER=gemini (the default "mock"
# provider needs no network and no key). Imported lazily in app/llm/client.py.
google-genai>=1.0
